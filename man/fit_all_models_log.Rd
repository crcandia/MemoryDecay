% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_decay_models.R
\name{fit_all_models_log}
\alias{fit_all_models_log}
\title{Fit All Log-Transformed Decay Models: Biexponential, Log-Normal, Exponential}
\usage{
fit_all_models_log(
  data,
  age_var = "age",
  observed_col = "CurrentPopularity",
  group_var = NULL,
  group_var2 = NULL,
  N_ref = NULL,
  weight_early_points = TRUE
)
}
\arguments{
\item{data}{A data frame with time-series or cross-sectional data (e.g., memory or attention scores).}

\item{age_var}{Name of the column representing age (e.g., time since event or figure).}

\item{observed_col}{Name of the observed response variable to be modeled (e.g., "loess_correct").}

\item{group_var}{Optional: first grouping variable (e.g., "same_country", "decay_bin").}

\item{group_var2}{Optional: second grouping variable (e.g., attention level, quantiles).}

\item{N_ref}{Optional: fixed value for parameter \eqn{N} in the biexponential model. If \code{NULL}, it will be estimated.}

\item{weight_early_points}{Logical. If \code{TRUE}, assigns higher weight to early observations to prioritize early decay (default: \code{TRUE}).}
}
\value{
A named list with the following components:
\describe{
  \item{biexponential}{A data frame of fitted values and parameters for the biexponential model.}
  \item{lognormal}{Fitted results for the log-normal modulated power-law model.}
  \item{exponential}{Fitted results for the exponential decay model.}
  \item{model_comparison}{Model comparison table including AIC and BIC for each fitted model.}
}
}
\description{
Fits three theoretical memory decay models to time-dependent attention or recall data:
}
\details{
\itemize{
  \item \strong{Biexponential Decay:}
  \deqn{
  S(t) = N \left[ \exp(-(p + r)t) + \frac{r}{p + r - q} \left( \exp(-qt) - \exp(-(p + r)t) \right) \right]
  }

  \item \strong{Log-Normal Modulated Power Law:}
  \deqn{
  S(t) = \exp(b) \cdot t^{b_1} \cdot \exp\left(-b_2 (\log t)^2\right)
  }

  \item \strong{Exponential Decay:}
  \deqn{
  S(t) = c \cdot \exp(-qt)
  }
}

All models are fitted in log-transformed form using nonlinear least squares (NLS).
Grouped and stratified fitting is supported through one or two categorical variables.
}
\seealso{
\code{\link{fit_biexponential_model}}, \code{\link{fit_lognormal_log_model}}, \code{\link{fit_exponential_log_model}}, \code{\link{plot_all_models}}
}
