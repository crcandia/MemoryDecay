% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_decay_models.R
\name{plot_fitted_decay_for_publication}
\alias{plot_fitted_decay_for_publication}
\title{Plot Fitted Decay Curve for Publication}
\usage{
plot_fitted_decay_for_publication(
  model_output,
  observed_col = "CurrentPopularity",
  fitted_col = "fitted_correct",
  age_var = "age",
  group_var = NULL,
  group_var2 = NULL,
  export_path = NULL,
  x_breaks = NULL,
  x_limits = NULL,
  y_breaks = NULL,
  y_limits = NULL,
  log_y = FALSE,
  log_x = FALSE
)
}
\arguments{
\item{model_output}{A list returned by a model fitting function (e.g., \code{\link{fit_biexponential_model}}),
containing a data frame named \code{fitted} with predicted values.}

\item{observed_col}{Name of the column containing observed attention/popularity values
(default: "CurrentPopularity").}

\item{fitted_col}{Name of the column containing fitted model predictions
(default: "fitted_correct").}

\item{age_var}{Name of the column representing age or time since event (default: "age").}

\item{group_var}{Optional. A grouping variable to color lines and points (e.g., "same_country").}

\item{group_var2}{Optional. A second grouping variable for faceting (e.g., attention level or quantile).}

\item{export_path}{Optional file path (without extension). If provided, saves both PDF and SVG outputs.}

\item{x_breaks}{Optional numeric vector of X-axis breaks.}

\item{x_limits}{Optional numeric vector of length 2 to set X-axis limits.}

\item{y_breaks}{Optional numeric vector of Y-axis breaks.}

\item{y_limits}{Optional numeric vector of length 2 to set Y-axis limits.}

\item{log_y}{Logical. If \code{TRUE}, uses log10 scale on Y-axis. Default is \code{FALSE}.}

\item{log_x}{Logical. If \code{TRUE}, uses log10 scale on X-axis. Default is \code{FALSE}.}
}
\value{
A \code{ggplot2} object suitable for academic publications.
}
\description{
Creates a high-quality plot comparing observed and fitted memory decay curves
for inclusion in publications. The plot highlights temporal decay patterns
in attention or popularity and supports grouping and faceting by categorical variables.
}
\details{
The function supports flexible axis scaling (log or linear), custom axis breaks and limits,
and exports to both PDF and SVG formats if desired.
}
\section{Requirements}{

This function requires the following packages:
\itemize{
  \item \code{ggplot2}
  \item \code{ggtext}
  \item \code{scales}
}
}

\seealso{
\code{\link{fit_all_models_log}}, \code{\link{plot_all_models}}, \code{\link{plot_raw_memory_decay}}
}
