% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_decay_models.R
\name{plot_fitted_decay}
\alias{plot_fitted_decay}
\title{Plot Fitted Memory Decay Curves Against Observed Data}
\usage{
plot_fitted_decay(
  model_output,
  observed_col = "CurrentPopularity",
  fitted_col = "fitted_correct",
  age_var = "age",
  group_var = NULL,
  group_var2 = NULL,
  log_y = TRUE,
  log_x = FALSE
)
}
\arguments{
\item{model_output}{Output from a model fitting function (e.g., \code{\link{fit_biexponential_model}}).}

\item{observed_col}{Name of the column containing observed values (default: "CurrentPopularity").}

\item{fitted_col}{Name of the column containing model predictions (default: "fitted_correct").}

\item{age_var}{Name of the age/time column (default: "age").}

\item{group_var}{Optional column for group-level coloring.}

\item{group_var2}{Optional column for faceting (e.g., stratification by attention).}

\item{log_y}{Logical. Use log10 scale on the y-axis? (default: TRUE).}

\item{log_x}{Logical. Use log10 scale on the x-axis? (default: FALSE).}
}
\value{
A \code{ggplot} object showing observed vs fitted decay curves.
}
\description{
Visualizes the fitted forgetting curve against observed attention or popularity values.
This function supports comparisons across groups and stratification by attention levels.
}
\section{Required Columns in the Fitted Data}{

\itemize{
  \item \code{age_var}: Numeric variable indicating time since the event or subject (e.g., years).
  \item \code{observed_col}: Observed attention/popularity (e.g., survey recall, citations).
  \item \code{fitted_col}: Predicted values from a decay model.
}
}

\section{Optional Columns}{

\itemize{
  \item \code{group_var}: Used to color lines/points by groups (e.g., same vs. different country).
  \item \code{group_var2}: Used to facet the plot by stratification variable (e.g., attention level).
}

Supports log-log visualization to capture long-tail decay or exponential behavior.
}

\seealso{
\code{\link{plot_all_models}}, \code{\link{fit_all_models_log}}, \code{\link{plot_fitted_decay_for_publication}}
}
